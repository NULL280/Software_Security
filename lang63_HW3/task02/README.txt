cat:
0x400862

flag:
0x4007c7

replace (by overflow buffer) the variable store cat with the add of flag, so the program run flag and displace the flag

The script (hw3task02.py) used to do that is stored in the same folder